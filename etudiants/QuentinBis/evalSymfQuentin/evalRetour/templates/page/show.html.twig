{% extends 'base.html.twig' %}

{% block title %}Page{% endblock %}

{% block body %}
    <h1 class="text-center">Page</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Titre</th>
            <td >{{ page.titre }}</td>
        </tr>

        <tr>
            <th>Categorie</th>
            <td>{{ page.categorie.nom }}</td>
        </tr>
        <tr>
            <th>Contenu</th>
            <td>{{ page.contenu }}</td>
        </tr>
        <tr>
            <th>DateCreation</th>
            <td>{{ page.dateCreation ? page.dateCreation|date('Y-m-d H:i:s') : '' }}</td>
        </tr>
        <th>Date de modif</th>
        <td>{{ page.dateCreation ? page.dateModification|date('Y-m-d H:i:s') : '' }}</td>
        </tr>
        </tbody>
    </table>




    <a class="btn btn-primary"  href="{{ path('app_page_index') }}">Retour à la liste</a>

{#    <a href="{{ path('app_page_edit', {'id': page.id}) }}">edit</a>#}

{#    {{ include('page/_delete_form.html.twig') }}#}
{% endblock %}
