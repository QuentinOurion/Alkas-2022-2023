{% extends 'base.html.twig' %}

{% block title %}{{ conversation.titre }}{% endblock %}

{% block body %}
    <article>
        <header><h1>{{ conversation.titre }}</h1></header>
        <div>{{ conversation.termine ? 'Yes' : 'No' }} {{ conversation.dateCreation|date('Y-m-d H:i:s') }} {{ conversation.dateModification ? conversation.dateModification|date('Y-m-d H:i:s') : '' }}</div>

        {% for message in conversation.messages %}
            {{ message.contenu }} {{ message.dateCreation|date('Y-m-d H:i:s') }} {{ message.dateModification ? message.dateModification|date('Y-m-d H:i:s') : '' }}
        {% else %}
            Il n'y a pas encore de message
        {% endfor %}


        <hr>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
        {{ form_end(form) }}
    </article>

    <a href="{{ path('app_conversation_index') }}">retour à la liste</a>

    <a href="{{ path('app_conversation_edit', {'id': conversation.id}) }}">modif</a>

    {{ include('conversation/_delete_form.html.twig') }}
{% endblock %}
